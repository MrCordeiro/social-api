# Social API

FastAPI tutorial that mimics a social network API

## Installation

```bash
pip install poetry
poetry install
```

## Usage

Start the web server:

```bash
uvicorn app.main:app --reload
```